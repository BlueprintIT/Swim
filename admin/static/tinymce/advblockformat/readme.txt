Provides a more advanced block format selector.
Pulls an xml file from the server in the same format as FCKEditor and
allows the user to select from a list.

Only supports block elements. Doesnt yet support style attributes.
